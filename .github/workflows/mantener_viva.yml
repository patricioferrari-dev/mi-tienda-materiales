name: Mantener Mis Apps Despiertas

on:
  schedule:
    # Se ejecuta cada 12 horas (00:00 y 12:00 UTC)
    - cron: '0 */12 * * *'
  workflow_dispatch: # Esto permite activarlo manualmente para probar

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Visitar las Apps de Streamlit
        run: |
          curl -I -s https://stock-jt96hgl3cncj8zyyzmgvp7.streamlit.app/
          curl -I -s https://m4anaxelfe3earcdygrk5u.streamlit.app/
